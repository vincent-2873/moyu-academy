# HANDOFF v14 — moyu-academy(2026-05-02 第十二輪 final · Wave 7+7.5 ship + Anthropic credit 卡點)

> **下個 Claude 進來先讀這份**。Vincent 在這輪改了 framing(Claude 是 CEO+COO+CTO,人類是 backup),Wave 7+7.5 全 ship 但卡 Anthropic API 餘額 sync。

---

## 🎯 進場 60 秒指令

```bash
# 1. login Vincent chrome
vincent@xuemi.co / 0000

# 2. 進 hub 看新 Claude 報告書
https://moyusales.zeabur.app/admin/hub
# 應該看到 FROM Claude / TO 董事會 信頭 + 北極星 banner + sparkline + 等拍板 cards

# 3. 看 Anthropic credit 是否通了
fetch('/api/cron/claude-daily-narrative?key=manual-trigger', {method:'POST'}).then(r=>r.json())
# 預期(通了):{ok:true, headline:"...", forecast:..., progress_pct:...}
# 實際(沒通):{ok:false, error:"credit balance is too low..."}

# 4. 如果通了 → 立刻 trigger 一輪 memo + 試 reject v2 閉環,截圖給 Vincent
# 如果沒通 → 看 Anthropic chat 有沒新回覆(Conversation ID: 215474149989560)
```

---

## 🚨 關鍵卡點:Anthropic API credit too low(問題在 Anthropic 端)

### 事實鏈
1. Vincent 今天 5/2 充值 $15 + $5 = **$20 USD**(Stripe paid 成功)
2. Anthropic Console 顯:**Remaining balance US$20.00**(Expiring May 3, 2027)
3. **但 prod API call 持續回 "credit balance is too low"** 已 25+ 分鐘
4. **Workbench(console 自家)也 fail** — `You are out of credits` ← 這是 ground truth

### 排除完畢的「我們這邊」
| 可能性 | 排除狀態 | 證據 |
|---|---|---|
| API key 串錯 | ✅ | prod env key 跟 console 此 org 的 key **byte-equal**(paste 後「儲存」 disabled = 同 key) |
| 多 organization 切錯 | ✅ | 只有 1 個 org `page.cinhong's Individual Org`(Members:1,sidebar 無 org switcher) |
| API key revoked | ✅ | Workbench 用同 org 也 fail = org-level 問題,非 key-level |
| Tier 限制 | ✅ | Tier 2 達標 |
| Spend limit cap | ✅ | $0 / $500 大空 |
| prod 程式碼 bug | ✅ | Wave 7+7.5 已 verified live(decision approve API 200 OK) |
| Stripe charge 沒進 | ✅ | Invoice 顯 paid $15 + $5 active until 2027 |

### 真兇(Anthropic 端,3 種可能)
1. **內部 billing → API gate cache 不同步**(console 顯有錢,但 API gate cache 沒 sync)
2. **風控 hold 沒解**(早上 5/2 有 1 筆 failed $25 stripe charge,後來成功 $15+$5,風控可能標記 review)
3. **Stripe webhook 沒 fire 到 API gate**

任一種都**需要 Anthropic human agent 處理**。

### 我已送 support ticket
- **Conversation ID: 215474149989560**
- 路徑:Anthropic console 右下 Intercom chat → 同意條款 → 選「訂閱/帳單問題」 → type 完整 ticket
- AI agent 已回「我會為您轉接到真人專員」 → 已 escalate
- Vincent 等 email / chat 通知(`991171dd@gmail.com`)

### 下個 Claude 進來該做的
1. **先 retry trigger memo** 看 sync 是否過了(很可能已通)
2. **如果通了** → 立刻完整 verify wave 7.5(memo 3 段 voice + reject v2 閉環)→ 截圖給 Vincent
3. **如果沒通** → 進 https://platform.claude.com/ 點右下 chat bubble 看是否有 human agent 回覆 → 如有,follow up

---

## ✅ Wave 7 ship 完整(2026-05-02 中段)

### Schema (D29 + D30 已 apply)
- `users.persona_role` enum:`human_ops` / `board_audience` / `employee_sales` / `employee_legal` / `claude_executive`(預留)
  - `human_ops`: 2(Vincent + super_admin)
  - `employee_sales`: 38
  - `employee_legal`: 2
  - `board_audience`: 1(`investor.demo@board.moyu / 0000` 預留)
- `claude_daily_narrative` 表(每天 06:00 cron 寫一筆)
- `claude_predictions` 表(週/月預測 → 月底 fill actual → accuracy)
- `decision_records` 加 `outcome` / `outcome_verified_at` / `outcome_claude_assessment` / `evidence_refs`
- `board_inquiries` 加 `answered_with_evidence`
- D30 砍乾淨:hr_*@demo.moyu(3 帳號)/ moyuhunt brand(3 users)/ DROP recruits / recruit_funnel_stages / recruit_actions / recruit_pipeline / 砍 training_paths recruit / module_overrides moyuhunt

### Backend (10+ endpoints)
- `/api/cron/claude-daily-narrative`(每天 06:00 UTC 22:00 = 台北 06:00)
- `/api/admin/claude-report` unified API(narrative + 北極星 + 14天 sparkline + decisions + worker runs + inquiries)
- `/api/admin/board/inquiry/ask`(投資人質詢 RAG + Metabase 真資料 + rate-limit 5/24h)
- `/api/admin/decisions/[id]/approve`(category-specific handler:strategy/hr/operations/legal)
- `/api/admin/decisions/[id]/reject`(want_v2=true 觸發 closed-loop;false 寫 RAG common 教訓)
- `/api/admin/chairman-overview` 重寫 sales-only + xlab(砍 recruit funnel / sparring / moyuhunt)
- `/api/admin/hub` unified API(Wave 6 殘留)
- `/api/admin/board/period-summary?period=week|month|quarter`(Wave 5 殘留)

### UI/UX
- `/admin/hub` Claude 報告書 memo 風(Source Serif Pro / FROM Claude TO 董事會 / 北極星 + 14 天 SVG sparkline / Claude 寫 markdown narrative with inline citation / highlights/warnings callouts / pending decisions cards / 質詢 textarea / 24h ops stats / [展開戰況] drill 連結)
- 24 個 inline SVG icon (`src/app/admin/_icons.tsx`):Gavel / MessageQuote / Activity / TrendingUp/Down / AlertTriangle / Flame / Clock / CheckCircle / XCircle / Edit3 / Calendar / Building2 / Briefcase / Scale / GraduationCap / Bot / Users / Settings / Sparkles / FileText / Send / Loader / LayoutDashboard / ChevronDown/Right
- Decision card icon + urgency badge(critical 🔥 / high ⏰ / normal ●)
- approve / reject / edit button 真按鈕
- /board layout 殼預留(投資人 portal — Wave 8 真實 ship)

### Cron schedule
- `cron.yml` 加 `0 22 * * *` UTC = 台北 06:00 跑 `claude-daily-narrative`
- 既有 Metabase rolling sync 每 15 min(weekday 9-22 台北)持續活

---

## ✅ Wave 7.5 ship 完整(closed-loop reject v2)

### Vincent 拍板(Q1 A+B / Q2 B / Q3 A+B+C)
1. **A: closed-loop reject** — Claude 看完 reject reason 立刻產 v2 提案
2. **B: toggle 讓 Vincent 選** 「給 v2」(預設打勾) / 「議題 dead」
3. **C: memo 3 段 voice 混合**:headline(黃仁勳)+ body(Bezos)+ closing(Buffett)
4. **補:UX toast + fade-out animation**(解「按下去沒發生」感)

### Reject endpoint 升級
- 收 `want_v2: boolean`(預設 true)
- want_v2=true:call Claude API 帶(原 decision + Vincent reason)→ Claude 產 v2 → INSERT 新 decision_records row(parent_id, status='pending')→ 不寫 RAG 教訓(對話還沒 dead)
- want_v2=false:寫 `knowledge_chunks` pillar=common 「永久避開」教訓 → Claude 之後類似 pattern RAG 撈到自動避開
- v2 Claude prompt:強調「不要重複原方案、針對駁回原因直接回應、用同 category、提具體不同解法、說明 v1→v2 差異」

### Hub UI 升級
- Reject modal 加 toggle:`[V] 讓 Claude 看完原因再給一版 v2 提案`(米黃強調框)
- Approve / reject 成功後:
  - `setFadingId` 觸發 `.memo-decision[data-fading=true]` opacity 0.3 + slide-left animation 0.6s
  - `showToast()` 顯 success message:
    - approve: 「✓ 已核准:OOschool 重訓 · 已寫入 system_run_log...」
    - reject + v2: 「✓ 已駁回 · Claude 看完原因改了 v2: ...」
    - reject + dead: 「✓ 已駁回 · 我學到教訓,以後類似 pattern 不再提」
  - Toast 圓角 pill 飄底中 4.5s 自動消失

### Memo prompt 3 段 voice
```
## (黃仁勳)頭條
5/2 - nSchool +12 closures, 連 3 季新高。OOschool -32% MoM,本週砍量啟動重訓。

## (Bezos)本週觀察 + 我的處理
本週我觀察到一個結構性問題:OOschool 連兩個月低於目標 70%,
主因不是業務努力,而是 ESG 主題客群對舊腳本免疫
[依據:本月成交 119 筆 / 營收 NT$987 萬,SMD 4/1-30 vs 5/1-2]...

## (Buffett)致董事會
親愛的董事會夥伴 — 本週集團營收達標 87%。
我特別想分享一個值得警惕的訊號...
```

### MoM 嚴格切月
- 不再 rolling 7/30 days,改 **本月迄今 vs 上月同期 + 本週迄今 vs 上週迄今**(投資人最愛)

---

## 📦 Commit chain(Wave 6 → Wave 7.5)

```
d21bfd6  feat(hub v1): 重做 admin 門面 — 一頁看完不切換 + hydration fix(Wave 6)
8a157a7  fix(admin nav): 6 zone-root 全改 client redirect(hydration alias bug)
fe2cd9c  fix(D28): claude_help_requests 對齊 D18 真實 schema
08e7393  fix(D28): decision_records row 3 缺 NULL 對齊 10 columns
9504ec1  fix(admin): /admin server redirect 改 client redirect(nav alias bug)
7fcf5e6  feat(D29): persona_role + claude_daily_narrative + predictions schema
bbd6e65  feat(wave 6): Claude as Executive — 報告書 hub + daily narrative + 質詢 + 預測 + /board portal 殼
245c84e  feat(wave 7 hot-fix): 砍 HR/招募殘留 + chairman rewrite sales-only + xlab + 共用 inline SVG icons
a6da97b  feat(wave 7): decision approve/reject 串通 + hub icon swap + 3 段 memo voice
f54bcf6  fix(D30 v2): 用 DO $$ EXCEPTION 包每個 statement,table/column 不存在不擋整個 migration
62ce42f  feat(wave 7.5): 駁回閉環 — Claude 看 reason 立刻產 v2 提案 + UX toast/animation
```

main HEAD = `62ce42f`(2026-05-02)
Zeabur prod = live(運作中 21m+ ago)

---

## 🌳 Vincent 第十一+十二輪拍板的 framing(永久記住)

### 角色定位(全新 paradigm)
```
Claude (AI 執行者) ← 24h 自動運作 / 每天寫 narrative / 主動拍板小事
        ↑
human_ops ← Vincent (現任) + 接班人(預留 schema) — 補 Claude 連結斷點
        ↑
board_audience ← 投資人 / 董事 / CFO READ ONLY + 質詢
        ↑
employees ← 業務 / 法務 / 訓練生 SCOPED 自己的東西
```

### 設計鐵則
1. **Claude 是 default 執行者,人類是 backup(補連結斷點)**
2. **不寫死 vincent@xuemi.co** — 之後不同人類會接 human_ops
3. **後台第一頁不是 dashboard,是 Claude 寫的 memo**(投資人看 narrative,不看 KPI 卡)
4. **每個 claim 帶 inline citation `[依據:具體數字 + SMD 來源]`** — 投資人會質詢
5. **駁回必須給 Claude 改 v2 機會** — 閉環對話進化,不是 fire-and-forget
6. **MoM / WoW 嚴格切月** — 不要 rolling days(投資人看「2026 5月」就乾淨 5/1-31)
7. **icon 用 inline SVG**(不要 unicode glyph 糊糊感)
8. **真按鈕真表格** — `.ds-btn` `.ds-table` `.ds-stat`,不要 inline style
9. **HR / 招募 / moyuhunt 永久砍** — D30 已乾淨,將來不要復活

---

## ⏳ Wave 8 預定(下一輪做)

### 等 Anthropic API 通後立刻 verify(P0,5 分鐘工)
1. Trigger `/api/cron/claude-daily-narrative?key=manual-trigger` → 看 Claude 寫的 3 段 voice 真實長相 → 截圖給 Vincent
2. Re-seed pending decision(D28 SQL re-trigger 加 3 個 pending) → 點駁回 → 寫 reason → 等 Claude v2 出現 → 截圖閉環給 Vincent
3. Trigger `/api/admin/board/inquiry/ask` 投資人質詢 → 看 Claude 帶 RAG 引用回答

### Wave 8 Layer 1(2-3 hr 工)
4. **TimeRangePicker** 整頁聯動(今天/週/兩週/月/上月/季/自訂 + 嚴格切月)
   - hub + board + sales/dashboard 全部 listen 該 component
   - 切換時 hub North Star + 各 KPI + 5 brand 表 + Top 5 全重算
   - memo 不重產(綁今日)
5. **prediction 閉環 cron**(`/api/cron/claude-weekly-prediction`)
   - 每週一 09:00 跑:Claude 預測下週 calls / closures / revenue / conversion_rate
   - 寫 `claude_predictions` row,target_period='week-2026-W19' 等
   - 月底自動 fill actual_value + accuracy_pct
   - 投資人質詢「Claude 多準?」有真數字
6. **Settings/people 加會員管理 UI**(persona_role 切換 + 邀請新成員 + 移交 human_ops)
7. **/board portal read-only**(投資人視角)
   - 隱藏「核准 / 駁回」button
   - 加「Export 季度 PDF」button
   - LINE binding 季報通知

### Wave 8 Layer 2(深化,4-6 hr)
8. **Decision outcome 閉環** — approved 14 天後 Claude 自動 verify outcome → 寫 `outcome_claude_assessment`
9. **質詢的 evidence 完整** — Claude 答完帶 [click expand] 看完整 RAG chunk + SQL row
10. **Auto-iterate Claude prompt** — `auto-iterate-30min` cron 真的吃過去 7 天 reject reasons,調整自己 prompt(reject 的 pattern 避開)

---

## 🔑 環境 / IDs / URLs 快查

| 項目 | 值 |
|---|---|
| moyu prod URL | https://moyusales.zeabur.app |
| Vincent 登入 | `vincent@xuemi.co / 0000` |
| Zeabur project | `69d7e6b286ea5714a4e4d7a5` |
| Zeabur service | `69d7e7c886ea5714a4e4d7db` |
| Zeabur dashboard | https://zeabur.com/projects/69d7e6b286ea5714a4e4d7a5/services/69d7e7c886ea5714a4e4d7db |
| Supabase project | `nqegeidvsflkwllnfink`(寰策合併共用)|
| Supabase Studio | https://supabase.com/dashboard/project/nqegeidvsflkwllnfink/sql/new |
| GitHub | https://github.com/vincent-2873/moyu-academy |
| GitHub apply-migration | https://github.com/vincent-2873/moyu-academy/actions/workflows/apply-migration.yml |
| Anthropic console | https://platform.claude.com/ |
| Anthropic Org | `page.cinhong's Individual Org`(ID: `24ab8434-be0b-4b9b-bb4b-d9a451a913ff`)|
| Anthropic 帳號 | `991171dd@gmail.com`(page.cinhong) |
| Anthropic Conversation ID(open) | `215474149989560`(billing sync issue) |
| 本機 repo | `C:\Users\USER\OneDrive\桌面\Claude code\moyu-academy\` |
| Demo accounts | sales_manager / legal_manager / sales_staff / sales_rookie / legal_staff @demo.moyu / 0000 |
| 投資人 demo | `investor.demo@board.moyu / 0000`(D29 seed,persona_role=board_audience)|

---

## 🛡️ 紅線備忘(永遠優先)

從 `~/.claude/CLAUDE.md` 摘要:
1. **紅線 1 - secret 不進 chat** — 不 reveal API key,不 screenshot reveal modal,不 body.innerText.slice 撈;Vincent 給 clipboard 操作授權但 secret 不 print
2. **紅線 2 - 可以說「不確定」** — 不編造
3. **紅線 3 - 不可逆破壞性操作要先停**(rm -rf / DROP TABLE / force push)— 這輪 D30 DROP recruits 表 Vincent 親口拍板 OK
4. **紅線 4 - 願景模式不直接執行** — 這輪 Vincent 全部 spec mode 拍板,直接動工 OK

### 操作備忘
- **Apply SQL** 用 GitHub workflow(coord click 945,245)— Supabase Studio Monaco editor 對 React 有 sync 問題
- **Trigger memo** 從 prod console:`fetch('/api/cron/claude-daily-narrative?key=manual-trigger', {method:'POST'}).then(r=>r.json())`
- **改 Zeabur env**:Zeabur dashboard → 環境變數 tab(coord 178,45)→ 編輯 pencil(coord 1011,103 for ANTHROPIC_API_KEY 但要 scroll)→ ctrl+a + Delete + ctrl+v → 「儲存」 → 重新部署
- **Anthropic Intercom chat**:console 右下 chat bubble → contentEditable composer 用 `computer.type` 直接 type(中文 OK)→ aria-label「傳送訊息…」 button click

---

## 📊 系統真實進度(對標 system-tree v2)

| 區 | 狀態 | 真實能用度 |
|---|---|---|
| 🏯 戰情中心 hub | Wave 6 ship + Wave 7 icon swap + Wave 7.5 toast | **80%**(等 API 通看 memo 就 90%) |
| 🏛️ board/quarterly | D26 schema + D28 demo seed + period-summary API | 60%(memo 通才到 90%) |
| 🏛️ board/strategy | API stub | 30% |
| 🏛️ board/inquiry | endpoint 真實串 RAG 等 API | 60% |
| 🏛️ board/decisions | Wave 7 approve/reject 真串通 + Wave 7.5 v2 閉環 | **85%**(等 API 通看 v2 就 95%) |
| 📊 sales/dashboard | chairman fix + 5 brand 真資料 | 80% |
| 📊 sales/individual | 真資料 | 70% |
| ⚖️ legal/cases | 3 cases 真資料 | 70% |
| ⚖️ legal/knowledge | 161 chunks RAG | 70% |
| 📚 training-ops/* | 4 子頁存在但資料淺 | 30% |
| 🤖 claude/live | 17 worker 顯但 system_run_log 部分 | 50% |
| 🤖 claude/log/knowledge/rules/personas | wrap 既有 component | 60% |
| 🤝 human/sos | 2 demo seed | 50% |
| 🤝 human/sign-off | 同 decisions | 50% |
| 🤝 human/arbitration | 1 demo seed | 50% |
| ⚙️ settings/people | 待 Wave 8 加 persona_role 切換 | 60% |
| ⚙️ settings/cron | 17 cron config | 50% |
| ⚙️ settings/health | 6227 row 真資料 | 80% |

**整體系統 ~65%**(等 API 通 + Wave 8 ship 後到 85%)

---

最後更新:2026-05-02 第十二輪 final · v14
卡點:Anthropic API credit too low(等 human agent 處理 Conversation 215474149989560)
main HEAD = `62ce42f`,prod live
context window 用得很滿,Vincent 要壓縮。下個 Claude 接手讀這份就能繼續。
