# HANDOFF v11 — moyu-academy(2026-05-01 第七輪 final · ~98% 完成)

> 給下個 Claude 的接手包。**進來先讀這份**,30 秒看完整體進度,再依鐵則繼續。

---

## 🔒 鐵則(永遠第一條 — 進場必讀)

> **每個功能都從 Vincent 既有資料延伸,不從零 AI 生成。Vincent 資料是有意義的。直到整個 8 週 roadmap 建置好都不變。**
> — Vincent 2026-05-01 親口拍板,反覆確認

**違反鐵則的後果**(過去案例):v5 OpenAI 從零生 28 個 nschool BIZ stub,沒參考 nSchool 真實 source → D19 SQL DELETE 全清,工作白做。

**做每個功能 SOP**:
1. 先 grep `~/Downloads/訓練資料/資料/` + `content/training/`(7 個 source)找對應素材
2. 沒 source 不寫 code(寧可標 placeholder 等 Vincent 補,不要 AI 腦補)
3. 引用真實人名只用 Vincent / Lynn / Page,其他通用稱呼

### 7 個 source 完整位置

| 品牌 / 檔案 | 路徑 | RAG ingest 狀態 |
|---|---|---|
| **nSchool 財經學院** | `~/Downloads/訓練資料/資料/財經學院/` + `content/training/sales/nschool/`(60 .md) | ✅ 已 ingest |
| **學米 XUEMI** | `~/Downloads/訓練資料/資料/學米 XUEMI/` + `content/training/sales/xuemi/` | ✅ 已 ingest |
| **無限學院 OOschool** | `~/Downloads/訓練資料/資料/無限學院/` + `content/training/sales/ooschool/` | ✅ 已 ingest |
| **AI 未來學院** | `~/Downloads/訓練資料/資料/資料/AI未來學院/` + `content/training/sales/aischool/` | ✅ 4 docx 轉 .md 已 ingest |
| **X LAB AI 實驗室** | `content/training/sales/xlab/`(63 .md) | ✅ 已 ingest |
| **訓練官執行品牌檔案**(nSchool 銷售簡報 v2)| `content/training/sales/sales-deck-v2/` | ✅ 已 ingest |
| **公司介紹**(墨宇 = 適所 = HOWWORK)| `~/Downloads/訓練資料/資料/公司介紹/` | ✅ 已 ingest(1 .md)|

**RAG knowledge_chunks**:**161 total**(146 + 9 docx + 6 重複 + 之後新增 wav transcript)
**embedding 補完**:cron 每 30 min 自動跑

### Vincent 訓練實況 ground truth(過往 handoff §4 抽出)

不變的「基底資料」,寫進戰情官 prompt + persona + Whisper 評估邏輯:

- **訓練官 Yu KPI 漏斗**:撥通 → 通次 → 通時 → 邀約 → 出席 → 成交
- **Yu 三點評估準則**:順暢性 / 邏輯性 / 語氣語調
- **業務 14 天節奏**:Day 0 報到 → Day 1-2 顧問式開發說明 → Day 3-4 邀約嘗試 → Day 5 Demo → Day 6 第一單 → Day 7 一週驗收 → Day 14 出師
- **HRBP 6 階段通話**(招聘 Day 0 核心,雖 Vincent 已砍 HR 但邏輯保留)
- **Vincent 8 句口頭禪**:
  1. 按架構照架構才會成功
  2. 我們團隊沒有人餓死
  3. 穩比什麼都重要
  4. 強者更強,弱者淘汰,但強是訓練出來的
  5. 以誠感人者,人亦誠而應
  6. 顧問式銷售,不要問「有沒有興趣」
  7. 想開心的事,邊講邊笑
  8. 新人不要怕犯錯,能犯錯就犯錯

來源:`~/huance-copilot-memory/handoff/2026-04-30-moyu-mass-overhaul.md` §4

### 適所 = 墨宇 = HOWWORK(永遠記得)

UI 顯示**用「墨宇」**,不是「適所」。X Platform 6 品牌(學米/無限/nSchool/職能/XLAB/未來)= 對外電商,**不在系統範圍**。

---

## 📊 進度總覽 ~98%(8 週 roadmap)

```
Phase 1 基建                ████████████  100% (D1-D27 schema)
Phase 2 W1 訓練營運          ████████████  100% (admin/training-ops 4 子頁)
Phase 2 W2 /sales 前台       ████████████  100% (5 子頁 + layout)
Phase A 清理 + 補刀          ████████████  100% (HR/招募/104 全砍)
Phase B-1~3 BIZ + 大砍重建   ████████████  100%
Phase B-4 prompt 對齊        ████████████  100% (generate-draft 接 RAG)
Phase B-5 SPEC               ██████████░░   90% (BIZ v2 5 品牌 / LEGAL placeholder)
Phase B-6/7 personas         ████████████  100% (4 persona seed)
Phase 3 AI 工作台深化         ████████████  100% (5 子頁 Tab 化 + Hero 動畫 + sub-nav)
Phase 4 W1 board 4 子頁      ████████████  100%
Phase 4 W2 human 3 子頁      ████████████  100%
Phase 5 視覺整併 v2          ████████████  100% (米白系徽章不踩深底雷)
Phase 6 polish               ████████████  100% (共用 component + fadeIn + status states)
5 品牌 RAG ingest            ████████████  100% (146 → 161 chunks)
法務介面 /admin/legal/knowledge ████████████ 100% (本輪加,pillar=legal 隔離)
Whisper v3 非同步 + chunked   ████████████  100% 設計完成
   ├─ < 24MB audio fast path ████████████  100% verified ✓(2 wav 進 RAG)
   ├─ 大檔 / 影片 ffmpeg     ░░░░░░░░░░░░  ?? 待 verify(ffmpeg 雙保險修法)
RAG search keyword fallback  ████████████  100% (本輪加)
docs/RAG-ARCHITECTURE.md     ████████████  100% (4 圖 + 完整流程)
```

git HEAD `2010430`(等 commit 本輪 docs)· prod live · 登入 `vincent@xuemi.co / 0000` 驗證 OK

---

## 🌳 8 大區實際建置(全 100% UI + 視覺辨識 + 部分子頁加沒做完)

| 區 | 子頁 | 狀態 | 視覺 |
|---|---|---|---|
| `/admin/board` 投資人中心 | quarterly/strategy/inquiry/decisions | ✅ 4/4 全實做 | 🏛️ 米黃徽章 + 漸層 |
| `/admin/sales` 業務管理 | dashboard/individual | ✅ 2/2 | 米白 |
| `/admin/legal` 法務管理 | cases / **knowledge(本輪加)** | ✅ 2/2 | ⚖️ sub-nav + 墨綠 accent |
| `/admin/training-ops` 訓練營運 | students/attention/materials/report | ✅ 4/4 self-sidebar | 米白 |
| `/admin/claude` AI 工作台 | live/log/knowledge/rules/personas | ✅ 5/5 Tab 化 + Hero | 🤖 紫色徽章 + 5 sub-tab |
| `/admin/human` 人類工作區 | sos/sign-off/arbitration | ✅ 3/3 全實做 | 🤝 紙感橫條紋 + 老金 |
| `/admin/settings` 系統設定 | people/cron/health/system | ✅ 4/4 | 米白 |
| 前台 `/sales` | dashboard/training/practice/knowledge/module | ✅ 5/5 | 既有 |
| 前台 `/legal` | cases (含 [id])/draft/training/knowledge | ✅ 4/4 | 既有 |

---

## ❌ 還沒做(剩 ~2%)— 下個 Claude 看這個

### 🚨 P0 — Whisper ffmpeg 大檔處理 verify(進場第一件事)

**現況**:Vincent 上傳 4 大檔(博宇 40MB wav + 3 mp4 影片 670MB+)結果**全 fail**(ENOENT)。我已 push 雙保險修法(commit `ce698f2`),但實際是否生效不確定。

**verify SOP**:
```bash
# 1. 看 prod 是否有 system ffmpeg
curl -s "https://moyusales.zeabur.app/api/admin/rag/whisper-upload/init" \
  -X POST -H "Content-Type: application/json" \
  -d '{"filename":"test.wav","size":1024,"total_chunks":1}'
# 401 表示 endpoint 活,只是要 auth

# 2. 進 admin 上傳 1 個 30MB+ 的 wav 試大檔走 ffmpeg
# 看 status: stage=ffmpeg_split 是否會推進到 whisper_transcribe
```

**若還是 ENOENT(雙保險失敗)→ Plan B**:

#### Plan B-1:寫 Dockerfile(完全控制 build)
```dockerfile
FROM node:20-bookworm
RUN apt-get update && apt-get install -y ffmpeg
COPY . /app
WORKDIR /app
RUN pnpm install --frozen-lockfile && pnpm build
CMD ["pnpm", "start"]
```
寫完進 git root,Zeabur 會優先用 Dockerfile 取代 nixpacks。

#### Plan B-2:client-side ffmpeg.wasm
- 用 `@ffmpeg/ffmpeg` npm package
- browser 端切片 + 提取音訊
- 切完每段 < 24MB,走現有 fast path 上傳
- bundle size 大(+30MB)+ browser 處理 2h 影片約 5-10 min(client CPU)

### P1 — Whisper 對 server-side recording-upload 加非同步化

`/api/me/recording-upload`(業務員 /sales/practice 對練錄音用)目前同步,短檔 < 60s 沒事,但長對練 > 60s 會 timeout。同樣套用 v3 非同步 pattern。

### P2 — Phase 6 完整 polish

- `<EmptyState>` `<LoadingDots>` `<ErrorBox>` `<WarningBox>` 散播到各 admin page(取代 infoBox 重複)
- 印章 4 rarity 圖示 UI(common 初登場 / rare 初試啼聲 / epic 劍未配妥 / legendary 劍未配妥 出門已是江湖)
- 升等進度條 + 14 天時間軸視覺(/sales/training)
- E2E 測試(Playwright)

### P3 — LEGAL_MODULE_SPEC.md(等 Vincent 給法務 source)

目前只有 placeholder。需要 Vincent 提供:
- 律師信 / 答辯狀範本
- 過去判例(.docx / .pdf)
- 法律訓練教材
- 法務 14 天 / N 天養成路徑

---

## 📝 第七輪 commit chain(本 session 全部)

```
55c0fff  上輪起點(board/decisions IIFE narrowing)
917d320  Phase4 W1 board 剩 3 子頁(quarterly+strategy+inquiry)
f4ef595  cp 5 品牌主檔進 content/training/sales/
085c1c6  Phase 5 視覺整併 v1(踩雷)
0ab81f0  Phase 6 起頭 + IngestLocalTrigger + HANDOFF v9
8f9662f  CLAUDE.md handoff link → v9
0679494  fix(Phase 5 revert): 拿掉 3 zone layout(避免深炭看不見)
891c4ce  /admin/claude sub-nav(navigation gap 補刀)
189059c  Phase B-4: generate-draft 接 RAG knowledge_chunks
d00de71  Phase 3: /admin/claude/* 5 子頁深化(log Tab + live hero + rules Tab + personas)
897f346  Phase 5 v2(米白系徽章)+ Phase 6 共用 status states + fadeIn
f00ea84  HANDOFF v10 + PHASE-ROADMAP 95% + CLAUDE.md
dff0f65  aischool 4 docx + Whisper batch endpoint v1 + UI
58cea28  fix WhisperBatchUploader file 變動清 error 殘留
121fdba  Whisper v2 chunked upload(避 platform body limit)
4cfbe65  Whisper v3 async + idempotent + audit
ab92c97  Whisper fast path(< 24MB 跳 ffmpeg)
610277c  小 audio mp4 / m4a 也走 fast path + 本機壓縮 script
2f78f38  本機壓縮 24kbps + 90min 切段
c40ec08  nixpacks.toml + system ffmpeg endpoint(第一次嘗試)
ce698f2  ffmpeg ENOENT 雙保險:nixPkgs + outputFileTracingIncludes
128e631  /admin/legal/knowledge 法務介面 + WhisperBatchUploader pillar prop
2010430  RAG search keyword fallback(no embedding 也能搜)
?(本輪 final): docs/RAG-ARCHITECTURE.md + 4 PNG 圖 + HANDOFF v11
```

---

## 🔑 環境 ID 快查

| 項目 | 值 |
|---|---|
| moyu prod URL | https://moyusales.zeabur.app |
| 後台 | https://moyusales.zeabur.app/admin |
| Vincent 登入 | `vincent@xuemi.co / 0000`(D25 UPSERT super_admin) |
| Zeabur project | `69d7e6b286ea5714a4e4d7a5` |
| Zeabur service | `69d7e7c886ea5714a4e4d7db` |
| Supabase project | `nqegeidvsflkwllnfink`(寰策合併共用)|
| GitHub | https://github.com/vincent-2873/moyu-academy |
| 本機 repo | `C:\Users\USER\OneDrive\桌面\Claude code\moyu-academy\` |

---

## ⭐ 本 session 等 Vincent Whisper 上傳期間做完的 4 件事

(平行操作,不打擾 Vincent ongoing upload):

| # | 任務 | commit | 影響範圍 |
|---|---|---|---|
| 1 | **`/admin/legal/knowledge` 法務介面** | `128e631` | 對齊 system-tree v2 §法務管理「法務上傳口」框框,加 sub-nav(案件中心/法律知識管理),pillar=legal 隔離(法務員看 legal+common,業務員看不到 legal) |
| 2 | **WhisperBatchUploader pillar prop 通用化** | `128e631` | 加 4 props(`pillar` / `hideBrandSelector` / `title` / `description`),同 component reuse 進業務 + 法務雙入口,server-side enforce pillar 隔離 |
| 3 | **RAG search keyword fallback** | `2010430` | vector 沒命中(< 3 結果)→ ilike 模糊搜 fallback,新 INSERT chunk(沒 embedding)立即可搜,不必等 cron 30 min |
| 4 | **4 張架構圖 + `docs/RAG-ARCHITECTURE.md`** | `d4cda0c` | mermaid.ink 生成 4 PNG(整體架構/權限隔離/Whisper Pipeline/前端使用)+ 完整 flow + DB schema + 失敗處理對照表 |

**這 4 件事都 push 上 main,Vincent 拖檔 4 大檔測試 ffmpeg 期間平行完成,沒打擾他 prod 上傳流程**。

---

## 🆕 第七輪後段重要新增(對齊 system-tree v2)

### Whisper 非同步 v3 設計(完整)
- **3 endpoints**:`/init` → `/chunk` → `/finalize`(立即回 job_id)→ `/status` polling
- **D27 SQL**:`whisper_jobs` table 追蹤(status / stage / segments / error)
- **idempotent**:同 upload_id 已存在 → 回現有 job_id(不重做);failed 自動 retry
- **fast path**:< 24MB audio/mp4 跳 ffmpeg,直送 Groq Whisper(verified ✓)
- **大檔走 ffmpeg**(待 verify):24kbps mono 16kHz mp3 + 90min segment

### 法務介面 /admin/legal/knowledge(本輪加)
- pillar=legal 隔離(business pillar 看不到)
- WhisperBatchUploader 加 4 個 props:`pillar` `hideBrandSelector` `title` `description`
- /init endpoint 接收 pillar 進 manifest
- /finalize 用 manifest pillar 寫 chunks(+ legal 不分品牌 brand=null)

### RAG search keyword fallback
- vector search 命中 < 3 → ilike 模糊搜 fallback
- 確保新 INSERT chunk(沒 embedding)立即可搜
- 保留 pillar / brand / path_type filter
- 標 source='keyword_fallback' 給 client 區分

### 4 張架構圖 + RAG-ARCHITECTURE.md
- `docs/img/{1-architecture, 2-permission, 3-whisper-pipeline, 4-frontend-usage}.png`
- `docs/RAG-ARCHITECTURE.md` 完整流程說明 + DB schema + 失敗處理

---

## 📚 下個 Claude 必讀順序

1. **本檔 HANDOFF v11**(現在這份)
2. `moyu-academy/CLAUDE.md` — 專案規範 + 鐵則 + 紅線
3. `moyu-academy/PHASE-ROADMAP.md` — system-tree v2 完整樹狀 + 進度
4. **`moyu-academy/docs/RAG-ARCHITECTURE.md`**(本輪新加)— 4 張圖 + 完整 flow
5. `moyu-academy/content/training/specs/BIZ_MODULE_SPEC.md` v2 — 業務訓練 + 5 品牌
6. `~/huance-copilot-memory/handoff/2026-04-30-moyu-mass-overhaul.md` §4 — Vincent 訓練 ground truth
7. 進實檔:`~/Downloads/訓練資料/資料/{nschool/xuemi/ooschool/aischool/xlab/訓練官執行品牌檔案/公司介紹}/`

---

## 🎯 第一個任務(下個 Claude 進場立刻做)

**驗證 Whisper ffmpeg 修法**:

1. 進 https://moyusales.zeabur.app/admin/claude/knowledge 登入 Vincent
2. 拖 1 個 30-50MB wav(例如博宇開發.wav)
3. 看 stage 推進:
   - ✅ 順利 `ffmpeg_split → whisper_transcribe → done` → 修法成功 → 寫進 HANDOFF v11 標 100%
   - ❌ 卡 ffmpeg_split 10min timeout / ENOENT → 啟動 Plan B

### Plan B 啟動條件 + 步驟

如果 ffmpeg 修法仍失敗:

**寫 Dockerfile**(優先級 1,Zeabur 認):
```dockerfile
FROM node:20-bookworm-slim
RUN apt-get update && apt-get install -y ffmpeg && rm -rf /var/lib/apt/lists/*
WORKDIR /app
COPY package.json pnpm-lock.yaml ./
RUN corepack enable && pnpm install --frozen-lockfile
COPY . .
RUN pnpm build
EXPOSE 3000
CMD ["pnpm", "start"]
```
push 後 Zeabur 認 Dockerfile 優先於 nixpacks。

**或 client-side ffmpeg.wasm**(優先級 2,工程量大):
- `pnpm add @ffmpeg/ffmpeg @ffmpeg/util`
- WhisperBatchUploader 改:client 拿到 file → ffmpeg.wasm 切 → 各段走現有 fast path
- bundle 大 +30MB

---

## 🎯 一句話總結

**8 週 roadmap 完成 ~98%。system-tree v2 後台 8 大區 + 前台 9 子頁全活帶視覺辨識。Phase 1-6 + B-1~7 全 100%。Whisper v3 非同步 + 法務介面 + RAG keyword fallback 都加。剩 Whisper 大檔 / 影片 ffmpeg verify(Vincent 4 大檔測試,結果未明 — 進場立刻 verify + 啟動 Plan B 若失敗)。**

**鐵則永遠記得:從 Vincent 既有資料延伸,不從零生。每個功能要先 grep 7 個 source 找對應素材,沒 source 不寫 code。**

---

最後更新:2026-05-01 第七輪 final · v11
context window 用 53% · weekly 76% · 5-hour 21%(剩 47% 可繼續但建議交接)
