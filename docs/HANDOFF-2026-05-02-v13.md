# HANDOFF v13 — moyu-academy(2026-05-02 第九輪 · Metabase 數據串接 + chairman fix + 卡點移交)

> **進場順序**:這份 v13 → [v12](HANDOFF-2026-05-01-v12.md) → [v11](HANDOFF-2026-05-01-v11.md) → 專案 [PHASE-ROADMAP.md](moyu-academy/PHASE-ROADMAP.md)
> Vincent 親口指示:**做事不要中間問他、不要叫他親手拖檔、不要叫他複製 log,自己用他的 chrome 去操作 zeabur**。

---

## 🔥 進場立刻做(60 秒內)

### Step 1:看 Vincent 卡在哪 — chairman/ceo 全 0 row root cause

```javascript
// 在 https://moyusales.zeabur.app(任何已登入 admin 頁面)console:
fetch("/api/admin/debug/smd").then(r => r.json()).then(j => console.log(JSON.stringify(j, null, 2)))
```

**預期結果**(看 sales_metrics_daily 真實狀態):
- `total` 應該 ~6227
- `is_monthly_rollup` 分布(null / true / false)
- `chairman_filter_count` 應該 > 0(本輪卡點:撈 0)
- `may_filtered_count` + `may_sample_5` 看 5 月真實 row

### Step 2:根據 debug 結果修

可能 root cause:
1. **`is_monthly_rollup` 全部 row 都標 true / false** → filter 邏輯反掉
2. **column 名錯**(`appointments_show` 可能不存在,實際是別的名)
3. **`email is null` 太多** → not("email", "is", null) 過濾掉
4. **Supabase RLS 攔截** — 但用 service_role 不該卡

修法依結果動 `chairman-overview/route.ts` + `sales/individual/route.ts` 對應 filter。

### Step 3:Verify

```javascript
fetch("/api/admin/chairman-overview").then(r => r.json()).then(j => console.log({
  ds: j.data_source.sales_kpi,
  ed: j.effective_date,
  ed_is_today: j.effective_date_is_today,
  empire_today_calls: j.empire.total_calls_today,  // 應該 > 0
  brand_today_calls: j.sales_brands.map(b => ({id: b.id, today: b.today_calls, week: b.week_calls})),
}))
```

期待:某天 brand 有 today_calls > 0(latest workday 的數字)。

---

## 🚨 Vincent 給的鐵則(永遠優先)

### 1. **基於 Vincent 既有資料延伸,不從零 AI 生成**(2026-05-01 拍板)
做每個功能要先 grep 7 個 source(`~/Downloads/訓練資料/資料/`),沒 source 不寫 code。
詳見 v11/v12。

### 2. **不要叫 Vincent 親手做事 / 不要中間問**(2026-05-02 拍板)
- ❌ 「請你拖一下 .wav」
- ❌ 「能不能複製 build log 給我」
- ❌ 「我建議你選 P0 還是 P2」
- ✅ 自己用他的 chrome 去 zeabur dashboard 看 log
- ✅ 自己決定優先級執行
- ✅ 做到一個段落再回報結果(不要中間每步問)

### 3. **看 Zeabur log 用 chrome MCP**
URL: `https://zeabur.com/projects/69d7e6b286ea5714a4e4d7a5/services/69d7e7c886ea5714a4e4d7db`
Chrome session 已登入。點 Deployments → 失敗 row → Build Logs → 找 ERROR / FAILED。

---

## 📊 本輪做了什麼(成功 / 失敗 / 卡)

### ✅ 成功
- **chairman-overview / individual 改用 sales_metrics_daily**(5191906 prod live ✅)
  - 換表:`kpi_entries`(舊,沒 sync)→ `sales_metrics_daily`(Metabase 同步)
  - 加 `effectiveToday` fallback(週末/假日 today 沒資料 → latest workday)
  - column mapping:`appointments` → `appointments_show` / `valid_calls` → `connected`
- **清 4 個 admin component HR/recruit 殘留**:
  - `KnowledgeEngineEditor.tsx` / `RagReviewQueue.tsx` / `RagUploadPanel.tsx`(`Pillar` type 拿掉 `hr`)
  - `UsersEditor.tsx`(ROLES 拿掉 hr/recruiter/recruit_manager/recruit_rookie + PATHS 拿掉 recruit)
- **Metabase 資料 verified healthy**:6227 rows / 5/1 latest / 5 brand 全有(via `/api/admin/metabase/health`)

### ❌ 失敗 → 已 revert
- **Dockerfile + .dockerignore (Plan B for Whisper)**:
  - 30c60a 加 → build OOM 失敗
  - beddf79 加 NODE_OPTIONS=4096 → 還是 OOM
  - **e366911 revert** 砍 Dockerfile,回 nixpacks(5191906 nixpacks 已 verified ok)

### 🚨 卡點(下個 Claude P0)
- **chairman / ceo / individual API 都撈 0 row**,即使 5191906 chairman fix 已 live
- 但 `metabase-health` 顯示 6227 rows
- 推了 858c37f `/api/admin/debug/smd` debug endpoint 看真實狀態(deploy 中,等 ~3 min)
- **下個 Claude 先 trigger debug → 根據結果決定怎麼修**

---

## 📦 本輪 commit chain(按時序)

```
f1a0e01  feat: 補刀 5 品牌 525 .md + 3 stub 子頁深化(個人戰況/legal draft/training)
8b198b8  fix(rag-pillars): regex 對 relative path 失準 → backfill endpoint
cb28b18  docs(HANDOFF v12): commit 進 docs/
2d2a234  docs(HANDOFF v12): final 戰果驗證
─── 5/2 接手 ───
dfa0c03  fix: chairman/individual 改用 sales_metrics_daily + fallback ❌ OOM
4ae051a  chore: force redeploy ❌ OOM
5191906  fix(types): Pillar 拿掉 hr 後 inline cast ✅ live(prod 跑此版)
030c60a  feat: Dockerfile + .dockerignore (Plan B) ❌ OOM
beddf79  fix(Dockerfile): NODE_OPTIONS=4096 ❌ 還 OOM
e366911  revert: 砍 Dockerfile 回 nixpacks ✅
858c37f  debug: /api/admin/debug/smd 看 SMD 真實 filter 結果 ⏳ deploy 中
```

prod HEAD = 5191906(+ 之後的 e366911 / 858c37f 應該也 deploy 上去,nixpacks 可能 OOM 風險還在)

---

## 🚧 還沒做 / 待解(優先順序)

| P | 任務 | 為什麼卡 | 怎麼做 |
|---|---|---|---|
| **P0** | chairman/ceo 0 row debug | 不知 root cause | trigger `/api/admin/debug/smd` |
| **P0** | OOM 風險 | 525 .md content build heap 爆 | 看是否要砍 noise / 或 nixpacks 加 NODE_OPTIONS |
| **P1** | Whisper ffmpeg 大檔 verify | Chrome MCP file_upload 被 deny / Plan B Dockerfile OOM | **不要叫 Vincent 拖**,改:client-side `@ffmpeg/ffmpeg` (ffmpeg.wasm) browser 端切 + 走現有 fast path |
| **P1** | aischool 3 wav 轉錄 | 依 P1 結果 | wav at `~/Downloads/訓練資料/資料/AI未來學院/{博宇,嘉賢,婉婷}.wav` |
| **P2** | nschool noise filter | 36 .md 是 Notion template(A Short Hike / Game Design 等) | grep 篩 keyword 排除非業務內容 |
| **P2** | LEGAL_MODULE_SPEC.md | 等 Vincent 給法務 source | (等)|
| **P3** | Phase 6 polish | 印章 4 rarity / 升等進度條 / 14天時間軸 / E2E | 規格在 system-tree v2 |

---

## 🔑 環境快查

| 項目 | 值 |
|---|---|
| moyu prod URL | https://moyusales.zeabur.app |
| Vincent 登入 | `vincent@xuemi.co / 0000`(D25 super_admin)|
| Zeabur project | `69d7e6b286ea5714a4e4d7a5` |
| Zeabur service | `69d7e7c886ea5714a4e4d7db` |
| Zeabur dashboard | https://zeabur.com/projects/69d7e6b286ea5714a4e4d7a5/services/69d7e7c886ea5714a4e4d7db |
| Supabase project | `nqegeidvsflkwllnfink`(寰策合併共用)|
| GitHub | https://github.com/vincent-2873/moyu-academy |
| 本機 repo | `C:\Users\USER\OneDrive\桌面\Claude code\moyu-academy\` |
| 訓練 source 7 位置 | `C:\Users\USER\Downloads\訓練資料\資料\{nschool/xuemi/ooschool/aischool/xlab/訓練官執行品牌檔案/公司介紹}\` |

---

## 🌳 system-tree v2 對齊狀態(同 v12)

8 大區 + 前台 9 子頁全活。Stub 真清單 = 0(全深化)。

RAG knowledge_chunks 狀態(v12 backfill 後):
- **sales pillar: ~593**(5 品牌真 source + 訓練官品牌 132)
- legal pillar: 2(待 Vincent 給法務 source)
- common: 34

---

## 💀 我這輪犯的錯(別重蹈)

1. **沒先看 Zeabur build log 就改 code**:第一個 dfa0c03 失敗時應該立刻 chrome MCP 進 zeabur dashboard 看 log,而不是猜 type error 推第二個 commit
2. **加 525 .md 沒考慮 build memory**:OOM 是必然,應該預先加 NODE_OPTIONS 或 chunk content
3. **Plan B Dockerfile 加複雜度沒幫助**:nixpacks 反而過,Dockerfile 路徑也 OOM,純粹增加干擾
4. **改完 chairman 沒 verify 0 row**:5191906 上線後我看到 `ds=sales_metrics_daily` 就以為 work,沒注意 `total_calls_today=0` — 這個應該立刻紅燈
5. **太多 commit 太亂**:9 個 commit 應該 squash 或合併。讓人看 git log 很難找重點

---

## 📝 給下個 Claude 的標準起手指令(Vincent 可直接複製)

```
你接手 moyu-academy 第九輪後段。先讀:
1. HANDOFF-2026-05-02-v13.md(本份,進場必讀)
2. HANDOFF-2026-05-01-v12.md(鐵則 + 訓練 source 補刀)
3. moyu-academy/CLAUDE.md(專案規範)
4. moyu-academy/PHASE-ROADMAP.md(8 週系統樹)

進場立刻做:
1. Chrome MCP login vincent@xuemi.co / 0000 (帳密在 v13)
2. console 跑 fetch("/api/admin/debug/smd").then(r=>r.json()).then(console.log)
3. 看 sales_metrics_daily 為什麼 chairman/ceo 撈 0 row
4. 找 root cause 修(可能是 is_monthly_rollup filter 反 / column 名錯 / RLS)
5. verify chairman-overview empire.total_calls_today > 0

別犯:
- 別叫 Vincent 親手拖檔 / 複製 log
- 改 code 失敗就立刻 chrome MCP 看 zeabur log,別猜
- 改完 verify endpoint 真實數值,別只看 deploy 成功
- 大改前先 NODE_OPTIONS / 看 build memory

看到 Vincent 訊息「我不會做任何事情」/ 「自己處理去操作看啦」 = 不要再叫他做手動操作。
```

---

最後更新:2026-05-02 第九輪 · v13(後段交接卡 chairman 0 row debug)
