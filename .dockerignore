# Dockerfile build context exclusions
node_modules
.next
.git
.env
.env.local
.env.production
.env.development

# 注意:NOT 排除 content/ — ingest-local-training 需要這些 .md / .txt
# 注意:NOT 排除 *.md root level(雖 root README.md 可能進 image,但無害)

# IDE / OS
.vscode
.idea
.DS_Store
Thumbs.db

# Build artifacts
dist
build
out
*.log

# Test files (build 不需要)
**/*.test.ts
**/*.test.tsx
**/*.spec.ts

# Old worker (本機 only,prod 不跑)
worker/

# Docs (跟 build 無關,瘦身用)
HANDOFF-*.md
docs/HANDOFF-*.md

# SQL 檔(prod 不執行)
supabase-migration-*.sql
